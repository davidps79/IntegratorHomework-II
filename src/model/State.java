package model;

/**
 * Enumeration with the states a subscriber can have.
 */
public enum State {
    /**
     * ACTIVE means the user is enabled to use the system.
     */
    ACTIVE, 
    /**
     * INACTIVE means the user is unabled to use the system.
     */
    INACTIVE;
}
